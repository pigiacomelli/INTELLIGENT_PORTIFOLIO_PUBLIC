{
    "name": "intelligent-portfolio-pro",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
        "setup": "npm run install:all && npm run migrate --prefix backend",
        "dev:backend": "cd backend && npm run dev",
        "dev:frontend": "cd frontend && npm run dev",
        "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
        "build": "npm run build --prefix backend && npm run build --prefix frontend",
        "db:backup": "npm run db:export --prefix backend",
        "prod": "concurrently \"npm run start --prefix backend\" \"npm run preview --prefix frontend\"",
        "start": "npm run dev"
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    },
    "dependencies": {
        "@google/genai": "^1.44.0",
        "helmet": "^8.1.0",
        "zod": "^4.3.6"
    }
}
